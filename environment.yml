name: dea-waterbodies-env
channels:
  - defaults
dependencies:
  - numpy
  - geopandas
  - gdal
  - pip:
    - datacube
    - fsspec==0.8.0
    - python-geohash
    - rioxarray
    - rasterstats
    - --extra-index-url="https://packages.dea.ga.gov.au"
    - odc-apps-dc-tools
